{
  "name": "crohnoz-forge",
  "version": "1.1.0",
  "private": true,
  "description": "Crohnoz Forge public blueprint experience and local-first Forge Studio workspace.",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run validate && npm test && npm run build"
  },
  "engines": { "node": ">=24" }
}
